/** @type {import('next').NextConfig} */
const nextConfig = {
    // assetPrefix: "https://aryanattapt.my.id/",
    // swcMinify: true,
    // trailingSlash: true,
    // images: {
    //     path: 'https://aryanattapt.my.id/_next/image',
    // },
    // webpack: (config) => {
    //     config.resolve.alias.canvas = false;
    //     return config;
    // },
};

export default nextConfig;
